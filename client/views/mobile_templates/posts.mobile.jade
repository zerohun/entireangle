template(name="PostsMobile")
  .row
    each posts
        +postMobile

template(name='postMobile')
  .col.s4
    .thumbnail-box
      a(href="/posts/{{_id}}")
       .loading-bg
         img.circle.responsive-img(src='{{thumbUrl imageId isVideo}}' data-src="/images/loading.gif")
       .post-info
         table
           tr
             th(rowspan='2')
               .right
                 +profileImage user
             td
               if title
                 | {{title}}
               else
                 | untitled
           tr
             td
               | {{user.username}}
